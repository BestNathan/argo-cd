apiVersion: v1
kind: Service
metadata:
  name: docs-rs-mcp
  namespace: mcp
  labels:
    app: docs-rs-mcp
spec:
  selector:
    app: docs-rs-mcp
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  type: ClusterIP
